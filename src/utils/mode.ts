export enum MeetMode {
  Typing = "TYPING",
  Talking = "TALKING",
}

export enum MeetModeFilterEnum {
  Typing = "Typing",
  Talking = "Talking",
  Both = "Both",
}

export enum SortInterviewsByEnum {
  NewToOld = "Interview: newest to oldest",
  OldToNew = "Interview: oldest to newest",
  DurHighToLow = "Interview length: long to short",
  DurLowToHigh = "Interview length: short to long",
}
